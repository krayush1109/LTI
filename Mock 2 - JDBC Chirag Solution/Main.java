
public class Main {
 
     public static void main(String[] args) {        
            // OrderDAO orderDAO = new OrderDAO();

            // // Create a new order
            // Order newOrder = new Order();
            // newOrder.setCustomerName("John Doe");
            // newOrder.setOrderTotal(99.99);
            // int orderId = orderDAO.createOrder(newOrder);
            // System.out.println("Created Order ID: " + orderId);

            // // Retrieve and print all orders
            // List<Order> orders = orderDAO.getAllOrders();
            // System.out.println("All Orders:");
            // for (Order order : orders) {
            //     System.out.println(order.getOrderId() + ": " + order.getCustomerName() + " - " + order.getOrderTotal());
            // }

            // // Retrieve a specific order by ID
            // Order order = orderDAO.getOrderById(orderId);
            // if (order != null) {
            //     System.out.println("Retrieved Order: " + order.getOrderId() + ": " + order.getCustomerName() + " - " + order.getOrderTotal());
            // }

            // // Update the order
            // order.setCustomerName("Jane Doe");
            // order.setOrderTotal(149.99);
            // Order updatedOrder = orderDAO.updateOrder(order);
            // if (updatedOrder != null) {
            //     System.out.println("Updated Order: " + updatedOrder.getOrderId() + ": " + updatedOrder.getCustomerName() + " - " + updatedOrder.getOrderTotal());
            // }

            // // Delete the order
            // orderDAO.deleteOrder(orderId);
            // System.out.println("Deleted Order ID: " + orderId);

            // // Confirm deletion by printing all orders again
            // orders = orderDAO.getAllOrders();
            // System.out.println("All Orders After Deletion:");
            // for (Order ord : orders) {
            //     System.out.println(ord.getOrderId() + ": " + ord.getCustomerName() + " - " + ord.getOrderTotal());
            // }

        
    }

}
